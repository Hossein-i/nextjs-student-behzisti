import { Root } from '@/views/root/ui';
import React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
interface RootPageProps {}

const RootPage: React.FC<RootPageProps> = () => {
  return <Root />;
};

export default RootPage;
